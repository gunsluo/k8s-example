apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storage-minio-pvc-minio-0
  namespace: storage
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-storage
